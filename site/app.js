const menuButton=document.querySelector('.nav-toggle');const menu=document.querySelector('.nav-links');if(menuButton&&menu){menuButton.addEventListener('click',()=>{const open=menu.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});menu.addEventListener('click',e=>{if(e.target instanceof HTMLAnchorElement){menu.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}})}document.querySelectorAll('[data-copy]').forEach(button=>{button.addEventListener('click',async()=>{const target=document.querySelector(button.getAttribute('data-copy'));if(!target)return;try{await navigator.clipboard.writeText(target.textContent.trim());button.textContent='Copied';setTimeout(()=>button.textContent='Copy',1400)}catch{button.textContent='Select';const range=document.createRange();range.selectNodeContents(target);const selection=window.getSelection();selection.removeAllRanges();selection.addRange(range)}})});const play=document.querySelector('[data-demo-play]');const rows=[...document.querySelectorAll('[data-demo-step]')];let timer=null;function resetDemo(){rows.forEach(row=>row.classList.remove('active'))}function playDemo(){if(timer)clearInterval(timer);resetDemo();let i=0;rows[0]?.classList.add('active');timer=setInterval(()=>{i+=1;if(i>=rows.length){clearInterval(timer);timer=null;return}rows[i].classList.add('active')},420)}if(play)play.addEventListener('click',playDemo);if(!window.matchMedia('(prefers-reduced-motion: reduce)').matches){const demo=document.querySelector('#demo');if(demo){const observer=new IntersectionObserver(entries=>{if(entries.some(e=>e.isIntersecting)){playDemo();observer.disconnect()}},{threshold:.35});observer.observe(demo)}}